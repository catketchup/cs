import numpy as np
# numpy is a library
import numpy.polynomial
# polynomial is a module
import numpy.polynomial.polynomial as pln
# the second polynomial is still a module
# there are instances and functions(methods) in the pln

# np.logical_and
# np.where
# np.sort
# np.array
# np.sum
# random.rand
# np.ravel
# np.meshgrid

# numpy.ndarray is a class
# define an numpy ndarray, we need a tuple (2,2)
x = np.ndarray((2, 2))
# or
y = np.ndarray(shape=(2, 2))
